<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dan.siimobile">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SIIMobile">
        <activity android:name=".Progress"></activity>
        <activity android:name=".Schedule" />
        <activity android:name=".Grades" />
        <activity android:name=".Residence" />
        <activity android:name=".ServicioSocial" />
        <activity android:name=".Notifications" />
        <activity android:name=".Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Profile" />
        <activity android:name=".Feed" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>